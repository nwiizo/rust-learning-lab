# リポジトリの運用方針

[English](AGENTS.md) | 日本語

## 共通の内容と言語

- 両ホストの入口は `plugins/rust-learning-lab/skills/learn-rust/SKILL.md`。スキルは一つに保ち、ホストや言語ごとに別実装を作らない。
- 通常のMarkdownは英語、`_ja.md` は日本語とする。意味を変えたら両方を更新し、相互リンクと同じ言語内の参照を保つ。`SKILL_ja.md` は読者向けの翻訳で、別の入口ではない。
- 回答言語は明示された希望、会話の順で判断する。英語のコードやエラーだけでは切り替えない。文書は指定や既存の慣例に合わせ、今作る成果物に必要な言語の資料だけを読む。
- コード、コマンド、API名、研究の数値と限界を翻訳間で保つ。対応するRustコードブロックは同じにする。リンクの表示名だけでなく、参照先の見出しも各言語に合わせる。

## 学習とレビューの動作

- 構文は目的、引数の対応、型、所有権、値の流れから説明する。言語仕様、APIの選択、確認できた歴史、推測を分ける。
- 直接の質問には先に答える。練習は任意とし、コードが動いたこと、説明したこと、自力で理解を示せたことを分ける。
- レビューは根拠のある指摘と観察できる影響を先に示す。詳しい学習レビューは設計判断・構文・検証を結ぶMarkdownに残せる。チャットのみ、読み取り専用、保存先の指定を守り、レビューからソース修正の許可を推定しない。
- 助言の理由と適用条件を保つ。一つの例や限定的な研究結果を一般的な規則にしない。詳しい条件付きの指針は補助資料へ置き、すべての回答や入口へ詰め込まない。
- 配布ファイルに非公開の原文、読書メモ、ローカルパス、記載しないと決めた帰属情報を入れない。応用する考え方は独自の指針として書き、研究上の主張を支える公開の一次資料への引用は保つ。

## 検証と公開

- 生成する教育向けレビューは `docs/rust-learning-review/` に置き、`.gitignore` と `.rgignore` で除外する。通常の実装・レビュー・検索・コンテキスト収集では、READMEの目次も含めてディレクトリ全体を対象外にする。除外設定に従わない検索ツールでは、パスを明示的に除外するかソースのディレクトリへ探索を絞る。明示的に指定されたレビューか、レビューの作成・更新に必要な目次と対象文書だけを読む。内容を自動的に読み込んだり、エージェントのメモリ・ルール・要約・製品の文書へ自動転記したりしない。
- PR・コミットから学ぶ文書には参照元と比較した版を記録する。作成・更新のたびに同ディレクトリの英語 `README.md` と日本語 `README_ja.md` の目次も更新し、ユーザーの文章と他の項目を保つ。教育用途であり、現在の動作やエージェントの指示を定める資料ではないことを説明する。生成する本文は指定された言語で書く。

- `git status` と `git diff` を確認し、無関係な変更を保ち、今回のファイルだけをステージする。コミット・push・公開は会話内の許可がある場合に行う。
- `.github/workflows/validate.yml` の配布メタデータ、日英のファイル対応、Rustコードの一致、両言語のdoctestを確認する。workflow変更時は `actionlint`、ローカルリンクは `lychee --offline --include-fragments --no-progress '**/*.md'` で確認する。
- 配布構成の変更後は両ホストのmanifestを検証する。静的検査とdoctestでモデルの振る舞い・レビュー精度・学習効果を実証したとは言わず、対話評価は実施した場合だけ記録する。
- 許可されたリリースの前に、両manifestのバージョン、秘密情報と記載除外情報、ブランチとremoteを確認する。公開済みタグは書き換えない。配布構成を変えた場合は公開marketplaceから両ホストへのインストールを確認する。
- インストール済みの更新はホストのmarketplace更新とplugin導入・更新コマンドを使う。キャッシュをGitへ入れず、導入版と内容を確認し、新しい会話や再起動が必要なら伝える。
